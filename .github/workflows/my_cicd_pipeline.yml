name: Test Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  docker-image-validation:
    runs-on: ubuntu-latest
    env:
      ENVIRONMENT: test
    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Initialize
        run: echo "Initializing environment: $ENVIRONMENT"

      - name: Validate Docker image
        run: |
          echo "Running Docker test validation..."
          # Example test: list images (replace this with your actual docker test commands)
          docker --version
          # Insert specific docker test commands below
          # e.g., docker build -t myimage .
          # e.g., docker run --rm myimage
