name: Test Pipeline

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - master

jobs:
  docker-image-validation:
    runs-on: ubuntu-latest
    env:
      ENVIRONMENT: test
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Initialize Test Environment
        run: |
          echo "Initializing environment: $ENVIRONMENT"

      - name: Validate Docker Image
        run: |
          echo "Running Docker image validation tests"
          # Replace below with your actual docker test (example)
          docker --version
          # Example: docker build -t test-image .
          # Example: docker run --rm test-image /bin/sh -c "your test command"
