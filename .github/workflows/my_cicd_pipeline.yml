name: Test Pipeline

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  docker-image-validation:
    runs-on: ubuntu-latest
    env:
      ENVIRONMENT: test
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Initialize environment
        run: echo "Setting environment to $ENVIRONMENT"

      - name: Validate Docker image
        run: |
          echo "Running Docker image validation of type: docker_test"
          # Example Docker test, replace with real validation commands if needed
          docker --version
          # If there are test scripts for Docker image validation, execute them below
          # ./scripts/docker_test.sh
