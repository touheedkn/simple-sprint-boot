name: Test Pipeline

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  test-docker:
    runs-on: ubuntu-latest
    env:
      ENVIRONMENT: test
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Initialize environment
        run: echo "Initializing environment: $ENVIRONMENT"

      - name: Validate Docker image
        run: |
          echo "Validating Docker image for type: docker_test"
          # Example Docker image testing command
          docker --version
          # Add actual Docker test commands here as required
