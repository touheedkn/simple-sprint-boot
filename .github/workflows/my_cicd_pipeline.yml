name: Test Pipeline

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  docker-image-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Initialize environment
        run: |
          echo "Setting up environment: test"
          # Export an environment variable, can be used in later steps
          echo "TEST_ENVIRONMENT=test" >> $GITHUB_ENV

      - name: Docker image validation
        run: |
          echo "Running Docker image tests of type: docker_test"
          # Example: list Docker images (could be replaced with actual tests)
          docker ps -a
          # Place your Docker test commands below
