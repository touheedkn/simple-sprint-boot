name: Test Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  docker-image-validation:
    runs-on: ubuntu-latest
    env:
      ENVIRONMENT: test
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Initialize Environment
        run: |
          echo "Initializing test environment"
          export ENVIRONMENT=test

      - name: Validate Docker Image
        run: |
          echo "Validating Docker image (docker_test)"
          # Example Docker tests. Replace with real commands as needed.
          docker version
          docker images
          # Insert docker testing logic below
          # e.g. docker build -t test-image .
          # docker run --rm test-image echo "Image test successful"