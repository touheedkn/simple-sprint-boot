name: Test Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      ENVIRONMENT: test
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Initialize environment (test)
        run: |
          echo "Setting up environment: $ENVIRONMENT"
          # Export environment variable for other steps if needed

      - name: Validate Docker image
        run: |
          echo "Running docker test validation..."
          # Check if Docker is available
          docker --version
          # Example Docker test - list images or run a test container
          docker images
          # (Add your docker validation logic here)
