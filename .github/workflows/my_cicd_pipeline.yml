name: Test Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  docker-image-test:
    runs-on: ubuntu-latest
    description: A simple test pipeline for Docker image validation

    steps:
      - name: Initialize Environment
        uses: actions/setup-environment@v1
        with:
          environment: test

      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Validate Docker Image
        run: |
          echo "Running Docker image validation"
          docker --version
          # Replace with appropriate docker test commands
