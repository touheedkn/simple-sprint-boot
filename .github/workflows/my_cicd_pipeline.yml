name: Test Pipeline

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  docker-image-validation:
    runs-on: ubuntu-latest
    description: A simple test pipeline for Docker image validation

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Initialize test environment
        run: echo "Setting up environment: test"

      - name: Validate Docker image
        run: |
          echo "Running Docker image validation test"
          # Example Docker test command; replace with actual validation as needed
          docker --version
