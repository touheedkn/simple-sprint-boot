name: Test Pipeline

on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  docker-image-test:
    runs-on: ubuntu-latest
    env:
      ENVIRONMENT: test
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Initialize Environment
        run: echo "Initializing environment: ${ENVIRONMENT}"

      - name: Validate Docker Image
        run: |
          echo "Validating Docker image (Type: docker_test)"
          # Example: Run docker commands or tests as needed below
          docker --version
          # You can add more docker validation as required
