name: Test Pipeline

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  docker-image-test:
    runs-on: ubuntu-latest
    env:
      ENVIRONMENT: test
    steps:
      - name: Initialize - Setup Test Environment
        uses: actions/checkout@v4

      - name: Set up environment variables
        run: echo "Environment: $ENVIRONMENT"

      - name: Validate - Docker Image Test
        run: |
          echo "Validating Docker image"
          # Example validation: List Docker images
          docker images
          # Insert actual docker test commands here. For example:
          # docker run --rm my-test-image:latest echo "Image runs successfully"
