name: Test Pipeline

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  docker-test:
    name: Docker Image Validation
    runs-on: ubuntu-latest

    env:
      ENVIRONMENT: test

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Initialize Environment
        run: |
          echo "Initializing environment: $ENVIRONMENT"

      - name: Validate Docker Image
        run: |
          echo "Running Docker test validation"
          # Example docker test command
          docker --version
          # Add your actual Docker image validation logic here (e.g., docker build & test)
