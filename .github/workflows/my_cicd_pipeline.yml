name: Test Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      ENVIRONMENT: test
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Initialize - Set environment to test
        run: echo "Environment set to $ENVIRONMENT"

      - name: Validate - Docker image test
        run: |
          echo "Running Docker image validation test of type: docker_test"
          # Example docker test command
          docker --version
          # Place your actual docker validation commands below
