name: Test Pipeline

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  docker-image-test:
    runs-on: ubuntu-latest
    env:
      ENVIRONMENT: test
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Initialize - Set up environment
        run: echo "Setting up environment: $ENVIRONMENT"

      - name: Validate - Docker image test
        run: |
          echo "Running Docker image validation of type: docker_test"
          # Example docker test commands
          docker --version
          # Add further docker test validation commands as needed
