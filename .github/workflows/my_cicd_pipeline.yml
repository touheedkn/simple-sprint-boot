name: Test Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  docker-image-validation:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Initialize environment for testing
        run: echo "Initializing environment: test"

      - name: Validate Docker image
        run: |
          echo "Running docker image validation tests..."
          # Add your docker image test/check commands here
          # Example:
          docker --version
          echo "Type of test: docker_test"
