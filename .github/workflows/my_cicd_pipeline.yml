name: Test Pipeline

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  docker-test:
    runs-on: ubuntu-latest
    env:
      ENVIRONMENT: test

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Initialize environment
        run: |
          echo "Initializing environment: $ENVIRONMENT"
          # Add relevant environment initialization commands here
          
      - name: Validate Docker image
        run: |
          echo "Validating Docker image..."
          docker --version
          docker info
          # Example test - list images (replace/add real docker test commands as needed)
          docker images
