name: Test Pipeline

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  docker-image-test:
    runs-on: ubuntu-latest
    env:
      ENVIRONMENT: test

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Initialize - Set environment variable
        run: |
          echo "Initializing test environment: $ENVIRONMENT"
        
      - name: Validate - Run Docker image validation
        run: |
          echo "Validating Docker image with type: docker_test"
          # Example docker command, replace with actual validation
          docker version
