name: Test Pipeline
on:
  push:
    branches:
      - main
  pull_request:

jobs:
  docker-test:
    runs-on: ubuntu-latest
    name: Docker Image Validation
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Initialize Environment
        env:
          ENVIRONMENT: test
        run: echo "Initializing environment: $ENVIRONMENT"

      - name: Validate Docker Image
        run: |
          echo "Running Docker image validation tests..."
          # Example: docker image inspection or running a container for testing
          docker info
          # Replace with your actual test, e.g.:
          # docker run --rm your-image:latest test-command
