name: Test Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  docker-image-validation:
    runs-on: ubuntu-latest
    env:
      ENVIRONMENT: test
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Initialize environment
        run: echo "Environment set to $ENVIRONMENT"

      - name: Validate Docker Image
        run: |
          echo "Running Docker tests for validation"
          # Example docker test commands
          docker --version
          # Place your docker image validation logic here
