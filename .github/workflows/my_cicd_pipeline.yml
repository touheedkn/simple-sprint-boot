name: Test Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      ENVIRONMENT: test

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Initialize environment
        run: echo "Initializing test environment."
        env:
          ENVIRONMENT: ${{ env.ENVIRONMENT }}

      - name: Validate Docker image
        run: |
          echo "Validating Docker image for testing..."
          # Example: Check if Docker is available
          docker --version
          # (You would add actual docker test commands here as needed)
