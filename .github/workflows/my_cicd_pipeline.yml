name: Test Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  docker-image-test:
    runs-on: ubuntu-latest
    env:
      ENVIRONMENT: test
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Initialize environment (setup)
        run: |
          echo "Setting up environment: $ENVIRONMENT"
          # Add any global test environment setup commands here

      - name: Validate Docker image (docker_test)
        run: |
          echo "Validating Docker image..."
          # Example Docker image validation:
          docker --version
          docker image ls
          # Replace with actual validation commands as needed
