name: Test Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Initialize environment
        run: echo "Initializing environment: test"

      - name: Validate Docker image
        run: |
          echo "Validating Docker image (type: docker_test)"
          # Example Docker test command, replace with actual validation logic if needed
          docker --version
