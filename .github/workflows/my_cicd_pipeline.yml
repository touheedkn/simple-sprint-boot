name: Test Pipeline

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  docker-image-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Initialize Environment
        env:
          ENVIRONMENT: test
        run: echo "Initializing environment: $ENVIRONMENT"

      - name: Validate Docker Image
        run: |
          echo "Validating Docker image (type: docker_test)"
          # Add your Docker image test logic here, e.g.:
          docker --version
          # Example placeholder for docker testing command
          # docker run --rm your-image:tag /bin/true
