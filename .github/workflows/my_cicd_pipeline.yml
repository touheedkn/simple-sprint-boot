name: Test Pipeline

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  docker-image-test:
    runs-on: ubuntu-latest
    description: A simple test pipeline for Docker image validation

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Initialize test environment
        run: echo "Initializing environment: test"

      - name: Validate Docker image
        run: |
          echo "Running Docker image test validation"
          # Example: List docker images (placeholder for real docker test)
          docker images
