name: Test Pipeline

on:
  push:
    branches: [main]
  pull_request:

jobs:
  docker-validation:
    runs-on: ubuntu-latest
    description: A simple test pipeline for Docker image validation

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Initialize Environment
        run: |
          echo "Setting up environment: test"
          # Example: Install dependencies or set environment variables
          # For demonstration, this just prints a message

      - name: Validate Docker Image
        run: |
          echo "Running docker image validation"
          docker --version
          # Example validation: attempt to build and run a test container
          docker build -t test-image .
          docker run --rm test-image
