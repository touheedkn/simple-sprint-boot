name: Test Pipeline

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  docker-image-validation:
    runs-on: ubuntu-latest
    env:
      ENVIRONMENT: test
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Initialize Environment
        run: |
          echo "Initializing environment for $ENVIRONMENT"

      - name: Validate Docker Image
        run: |
          echo "Running Docker test validation"
          # Replace the following line with your actual docker validation logic
          docker --version
