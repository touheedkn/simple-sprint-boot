name: Test Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  docker-image-validation:
    runs-on: ubuntu-latest
    description: A simple test pipeline for Docker image validation
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Initialize environment
        run: |
          echo "Initializing environment: test"
          # Set up environment variables or tools as needed
          export ENVIRONMENT=test

      - name: Validate Docker image
        run: |
          echo "Validating Docker image for test type: docker_test"
          # Example Docker image test - replace with your actual test commands
          docker --version
          # Optionally, run your own Docker container tests:
          # docker build -t test-image .
          # docker run --rm test-image
