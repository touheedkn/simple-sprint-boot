name: Test Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  docker-image-test:
    name: Docker Image Validation
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Initialize environment
        run: |
          echo "Setting up environment: test"
          # Example: export ENVIRONMENT=test
          echo "ENVIRONMENT=test" >> $GITHUB_ENV

      - name: Validate Docker image
        run: |
          echo "Validating Docker image for type: docker_test"
          # Example validation:
          docker --version
          # You could pull, build, or test an image as needed here
