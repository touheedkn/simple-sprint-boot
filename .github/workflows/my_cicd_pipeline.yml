name: Test Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  docker-validation:
    runs-on: ubuntu-latest
    description: A simple test pipeline for Docker image validation
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Initialize environment
        run: echo "Initializing environment: test"

      - name: Validate Docker image
        run: |
          echo "Running docker image validation"
          # Replace below with actual docker test logic, e.g., build or test
          docker --version
