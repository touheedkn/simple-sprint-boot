name: Test Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  docker-image-validation:
    runs-on: ubuntu-latest
    env:
      ENVIRONMENT: test
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Initialize Environment (Environment: test)
        run: |
          echo "Initializing environment: $ENVIRONMENT"
          # Add initialization scripts here as needed

      - name: Validate Docker Image
        run: |
          echo "Validating Docker image (type: docker_test)"
          # Example: List all docker images
          docker images
          # Insert additional steps here for docker image validation
