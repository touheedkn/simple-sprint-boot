name: Test Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  docker-image-validation:
    runs-on: ubuntu-latest
    env:
      ENVIRONMENT: test

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Initialize Environment
        run: echo "Initializing test environment..."
        env:
          ENVIRONMENT: ${{ env.ENVIRONMENT }}

      - name: Validate Docker Image
        run: |
          echo "Validating Docker image for test environment..."
          # Example docker test command, replace with actual image/test as needed
          docker --version
          # For actual validation replace the below command
          # docker run --rm my-image:latest test
