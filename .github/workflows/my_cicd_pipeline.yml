name: Test Pipeline

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  docker-image-test:
    runs-on: ubuntu-latest
    env:
      ENVIRONMENT: test
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Initialize Environment
        run: echo "Initializing environment: $ENVIRONMENT"

      - name: Validate Docker Image
        run: |
          echo "Validating Docker image test"
          # Replace this with actual docker test logic, e.g.:
          docker --version
          # Example: docker build -t test-image .
          # Example: docker run --rm test-image /bin/sh -c "echo running tests"
