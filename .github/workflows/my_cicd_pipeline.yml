name: Test Pipeline

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  docker-image-validation:
    runs-on: ubuntu-latest
    env:
      ENVIRONMENT: test

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Initialize environment
        run: echo "Initializing environment for $ENVIRONMENT"

      - name: Validate Docker image
        run: |
          echo "Validating Docker image using type: docker_test"
          # Replace this with your actual docker test logic, for example:
          docker version
          # Example test command (uncomment if suitable for your environment)
          # docker run --rm your-image:tag some-test-command
