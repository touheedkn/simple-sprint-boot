name: Test Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Initialize test environment
        run: |
          echo "Initializing test environment: test"
          # Add custom initialization commands here if needed

      - name: Validate Docker image
        run: |
          echo "Running Docker image validation (type: docker_test)"
          # Example docker command, replace with actual validation logic
          docker --version
          # docker run --rm my-image:latest test-command
