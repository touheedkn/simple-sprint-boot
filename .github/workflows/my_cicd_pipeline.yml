name: Test Pipeline

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Initialize
        run: |
          echo "Initializing environment: test"
        env:
          ENVIRONMENT: test

      - name: Validate Docker image
        run: |
          echo "Validating Docker image for type: docker_test"
          # Example: Pull/test a Docker image
          docker --version
          # Replace with actual test command as needed
          # docker pull your-image:tag
          # docker run --rm your-image:tag
