name: Test Pipeline

on:
  push:
    branches:
      - main
      - master
  pull_request:

jobs:
  docker-image-test:
    runs-on: ubuntu-latest
    env:
      ENVIRONMENT: test
    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Initialize Test Environment
        run: |
          echo "Initializing environment: $ENVIRONMENT"

      - name: Validate Docker Image
        run: |
          echo "Validating Docker image functionality (type: docker_test)"
          # Example: Pull and test a Docker image
          docker --version
          # If you want to test building a Docker image, uncomment below and adjust as needed:
          # docker build -t my-test-image .
          # docker run --rm my-test-image

