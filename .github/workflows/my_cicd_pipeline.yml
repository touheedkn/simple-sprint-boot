name: Test Pipeline

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  docker-validation:
    runs-on: ubuntu-latest
    env:
      ENVIRONMENT: test
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Initialize - Setup Environment
        run: |
          echo "Setting up environment: $ENVIRONMENT"

      - name: Validate - Docker Image Test
        run: |
          echo "Running Docker image validation tests..."
          # Example Docker test command, replace with your actual test suite
          docker --version
          # The next line could be:
          # docker run --rm your-test-image:latest
