name: Test Pipeline

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  docker-image-test:
    runs-on: ubuntu-latest
    name: Docker Image Validation

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Initialize environment
        run: |
          echo "Setting up environment: test"
          # Placeholder for additional environment initialization tasks

      - name: Validate Docker image
        run: |
          echo "Running Docker image validation tests..."
          docker --version
          # Example Docker test: check if image builds and runs
          docker build -t test-image .
          docker run --rm test-image
          # Add further validation commands as needed
