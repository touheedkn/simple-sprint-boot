name: Test Pipeline

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  docker-image-validation:
    runs-on: ubuntu-latest
    env:
      ENVIRONMENT: test
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Initialize environment
        run: echo "Setting environment to $ENVIRONMENT"

      - name: Validate Docker image
        run: |
          echo "Running Docker image test validation..."
          # Example docker test, replace with actual validation as needed
          docker --version
          docker ps
          echo "Docker test type: docker_test"
